#!/usr/bin/make -f

%:
	dh $@

override_dh_dwz:
override_dh_strip:
override_dh_makeshlibs:
dh_installsystemd -padaptation-garden-configs --name=modules.service:
dh_installsystemd -padaptation-garden-configs --name=brightness.service:
